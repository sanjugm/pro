apiVersion: v1
kind: Service

metadata:
  name: req4-api-service

spec:
  type: LoadBalancer

  selector:
    app: req4-api

  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP